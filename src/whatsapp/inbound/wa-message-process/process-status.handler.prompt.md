1.) If the `value.statuses.status` equals either `sent` or `read` it can be ignored and terminate the worker. 
2.) If the `value.statuses.status` equals `delivered` then delete all keys that start with `{wabot:${ENV}}:inflight:wa_id:<wa_id>` in Redis where wa_id (XXX) is equal to `value.statuses[x].recipient_id`. Here is an example command (though you wouldn't use this terminal command but the application code equivalent) `redis-cli --scan --pattern '{wabot:${ENV}}:inflight:wa_id:XXX*' | xargs -r redis-cli UNLINK`. PadhaiPal has now responded to the user so we don't need to block consecutive user messages anymore. 

* If this call to Redis fails then log a WARN, capture this in metrics, attach relevant information in the span metadata and attempt to call redis every 250ms for up to 25 seconds, logging a WARN for every failed attempt, etc. Note there is a small chance that the user will send a message after when redis starts working again and before this attempt occurs again, in that case the user's message will be treated incorrectly as a consecutive message and the user's second message will be treated incorrectly as the first message. I can have a metric for how many times this happens by querying Redis to see how many messages for that user exist before deleting those keys, if a key has been added in the duration of time that we have been trying to delete keys then we know that this failure mode has occurred. If the metrics show that this has happened too many times then increase 250ms to a lower value otherwise increase it to save on compute.
  * If the backoff cap is reached then log an ERROR and terminate the job. Note that these keys have a time to live (TTL) of 25 seconds anyway so there isn't any point in trying to delete them after that point.
3.) If the `value.statuses.status` equals `failed` then that message the bot tried to send to the user failed to send. Ideally we would figure out which message was sent and send it again however that seems fairly complicated and I don't know how often this error occurs so I'm just going to run sendFallbackMessage (see send-fallback-message.prompt.md). Mark this in the appropriate logs/metrics/traces as level WARN and attach all of the status[x].errors[] information. I am assuming this errors array only appears when status is failed but this is explicitly stated to be the case in the docs "<!-- only included if failure to send or deliver message -->"https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/reference/messages/status/?utm_source=chatgpt.com
